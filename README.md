Project Description

This project is a React.js web application where all the application data is fetched dynamically from REST APIs. The frontend is responsible for displaying the user interface, while the backend provides the data through API endpoints.

The application follows a component-based architecture, with reusable React components, state management, and API integration for real-time data.
